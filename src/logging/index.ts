/**
 * Logging subsystem for Boop.
 *
 * Provides structured logging to file and human-readable console output.
 * Derived from OpenClaw's logging module (MIT license).
 */
export { LogLevel, normalizeLogLevel, levelToMinLevel } from "./levels.js";
